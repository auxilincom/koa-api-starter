pipeline:
  run-api-starter-tests:
    image: node:8.12.0
    commands:
      - chmod +x ./bin/run.sh
      - ./bin/run.sh
    when:
      status: [ failure, success ]
      event: [ pull_request ]

services:
  mongo:
    image: mongo:4.0.3
branches: [ master, "*:master" ]
